[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "causa-sdk"
version = "0.1.0"
description = "Explicit event recording helpers for Causa tapes"
requires-python = ">=3.9"
dependencies = ["blake3>=0.4"]

[tool.setuptools]
py-modules = ["causa"]
